
# name: Show infomation

on:
  push:
    branches: [ '*' ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
     - name: Show github context
       run: echo '${{ toJson(github) }}'
     - name: Show memory
       run: free -h
     - name: Show cpu
       run: lscpu
